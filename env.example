# Copie este arquivo para ".env" na raiz do projeto e ajuste se precisar.
# O ".env" real NUNCA deve ser commitado.

# --- Banco de dados (PostgreSQL) ---
DB_USER=pi4_user
DB_PASSWORD=pi4_password
DB_NAME=pi4_db
DB_PORT=5432

# --- Backend (ASP.NET Core) ---
BACKEND_PORT=8000
